# Eat 5 star do nothing

# Embed-ize a text document and a query. Cosine similarity score is found
# Find highest score via enumeration & lambda function(or whatever way) and assign an index to it
# Print the text at this index, .'. Its the query answer